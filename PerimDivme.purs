module Main where

import Prelude

-- initial addme function 

addme :: Int -> Int -> Int 
addme a b = a + b 


-- divme function integers only
divme :: Int -> Int -> Int 
divme a b = a / b

-- perimeter function 
perim :: Int -> Int -> Int 
perim l w = (l * 2) + (w * 2)
